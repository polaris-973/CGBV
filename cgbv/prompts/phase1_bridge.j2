You are fixing a predicate vocabulary connectivity issue in a Z3-Python logical formalization.

## Full Original Formalization

```python
{{ raw_code }}
```

---

## Problem: Premise {{ premise_index }} is Structurally Disconnected

**Premise {{ premise_index }}** uses predicate(s) `{{ orphaned_preds | join(", ") }}` that do not
appear in any universal rule or the conclusion. The premise can **never** contribute to deriving
the conclusion — the proof chain is broken.

- NL text:          "{{ nl_text }}"
- Current formula:  `{{ current_formula }}`
- Orphaned predicate(s): `{{ orphaned_preds | join(", ") }}`

---

{% if retry_context %}
## Previous Bridge Rejected

{% if retry_context.reason == "entails_conclusion" %}
Your previous bridge was rejected because it **directly proves the conclusion** via the existing premises.

- **Rejected bridge**: `{{ retry_context.rejected_bridge }}`
- **Reason**: `premises ∪ {bridge}` makes `{{ conclusion_formula }}` unavoidable — Z3 confirms
  the conclusion is forced even before any grounding. This is conclusion injection and must NOT be used.

Generate a **different** bridge that:
1. Does **NOT** entail `{{ conclusion_formula }}` even when combined with ALL existing premises
2. Still connects `{{ orphaned_preds | join(", ") }}` to the proof chain via a semantically valid link
3. Does **NOT** contradict any existing premise

{% else %}
Your previous bridge was rejected because it creates a **logical contradiction** with the existing premises.

- **Rejected bridge**: `{{ retry_context.rejected_bridge }}`
- **Reason**: Adding this bridge makes the premise set unsatisfiable (Z3 reports UNSAT). This would
  cause Z3 to vacuously prove any conclusion — it must NOT be used.

Generate a **different** bridge that:
1. Does **NOT** contradict any existing premise
2. Still connects `{{ orphaned_preds | join(", ") }}` to the proof chain via a semantically valid link
3. Does **NOT** directly prove the conclusion `{{ conclusion_formula }}`
{% endif %}

If no valid alternative exists, output `NONE`.

---
{% endif %}
## Proof-Chain Context

**Conclusion to prove:**
- NL:      "{{ conclusion_nl }}"
- Formula: `{{ conclusion_formula }}`

**Connected predicates** (appear in connected premises or the conclusion):
`{{ connected_preds | join(", ") }}`

**Other premises (for context):**
{% for i, nl, fol in other_premises %}
Premise {{ i }}: "{{ nl }}"
  → `{{ fol }}`
{% endfor %}

---

## Your Task

Write a **linking axiom** — a universally-quantified rule that connects the orphaned
predicate(s) `{{ orphaned_preds | join(", ") }}` into the connected proof chain, so that
Premise {{ premise_index }} can participate in deriving the conclusion.

What general semantic relationship between the orphaned and connected predicates is
**directly supported by the NL text**?

**Examples of valid linking axioms:**
- Orphaned `is_cat`, connected `is_pet`:
  → `ForAll([x], Implies(is_cat(x), is_pet(x)))`
- Orphaned `applying_to_take(x, y)`, connected `gre_applicant`:
  → `ForAll([x], Implies(Exists([y], applying_to_take(x, y)), gre_applicant(x)))`
- Orphaned `is_mature_stock`, connected `is_stock`:
  → `ForAll([x], Implies(is_mature_stock(x), is_stock(x)))`

**When to output `NONE` (uppercase):**
- The NL text does not state or imply a general relationship between the orphaned and
  connected predicates — do NOT invent relationships without textual support.
- The only plausible link would directly target the conclusion predicate `{{ conclusion_formula }}`.
  A bridge that trivially proves the conclusion bypasses the proof chain and constitutes
  false premise injection — output `NONE` in this case.

---

## Output Rules

1. **Bridge guard**: Do NOT output a bridge whose consequent directly contains or trivially
   implies the conclusion formula `{{ conclusion_formula }}`. If the only plausible link leads
   directly to the conclusion predicate, output `NONE`.
2. The axiom MUST be universally quantified (`ForAll`/`Exists` form). Do NOT output a ground fact.
3. Use ONLY identifiers already declared in the code above. Do NOT introduce new names.
4. Output ONLY the Python expression (or `NONE`). No imports, assignments, comments, or prose.
5. The output (if not `NONE`) must be a valid Z3 `BoolRef` evaluable in the same namespace
   as the original code.
