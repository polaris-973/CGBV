You are fixing a predicate-connectivity gap in a JSON DSL formalization.

Output ONLY one JSON logic expression for a bridge rule, or `NONE`.
You may output either:
- a JSON string (compact logic DSL), or
- a JSON logic object.
Do NOT output Python.

## Current DSL

```json
{{ current_dsl }}
```

## Problem

- Premise index: {{ premise_index }}
- NL text: "{{ nl_text }}"
- Current compiled formula: `{{ current_formula }}`
- Orphaned predicates: `{{ orphaned_preds | join(", ") }}`
- Connected predicates: `{{ connected_preds | join(", ") }}`
- Conclusion NL: "{{ conclusion_nl }}"
- Conclusion formula: `{{ conclusion_formula }}`

{% if retry_context %}
## Previous Bridge Rejected

- Rejected bridge: `{{ retry_context.rejected_bridge }}`
- Reason: {{ retry_context.reason }}
{% endif %}

## Bridge Rules

1. Output one quantified bridge rule only (typically `forall ...` / `exists ...`).
2. Use only the existing symbol table from the current DSL.
3. The bridge must be a general quantified rule, not a ground fact.
4. If the NL does not license a valid bridge, output `NONE`.
5. Preferred compact logic syntax: `p(x)`, `f(x)==y`, `x!=y`, `and(...)`, `or(...)`, `not(...)`, `implies(a,b)`, `iff(a,b)`, `forall x:S. body`, `exists x:S. body`, `eq(a,b)`, `ne(a,b)`.
6. Never use assignment-style `"x = y"`; use `"x == y"`.
7. Never use literal boolean atoms like `"true"` or `"false"`.
8. Do not output legacy wrapper fields like `kind`, `constraint`, `forall`, `exists`.
