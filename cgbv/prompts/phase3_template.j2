You are grounding one natural-language sentence into ONE grounded-template IR JSON object.

Output ONE JSON object only.

## Frozen Symbol Table

```json
{{ symbol_context_json }}
```

## Current Sentence DSL Logic

```json
{{ sentence_logic_json }}
```

Core predicate/function vocabulary that should remain visible in the template:
{% if core_predicates %}`{{ core_predicates | join("`, `") }}`{% else %}`(none)`{% endif %}

## Domain Schema

{{ domain_schema }}

## Sentence

"{{ sentence }}"

## Sentence Index (Debug Context Only)

- `sentence_index`: `{{ sentence_index }}`

## Output Rules

1. Top-level field required:
   - `expr` (object)
2. Allowed `op` only:
   - Bool: `and`, `or`, `not`, `implies`, `iff`, `const_bool`
   - Atom: `truth`
   - Term: `value`, `const`, `var`
   - Compare: `eq`, `ne`, `lt`, `le`, `gt`, `ge`
   - Quantifier: `all`, `any`
3. `truth` shape:
   - `{"op":"truth","pred":"predicate_name","args":[TERM,...]}`
4. `value` shape:
   - `{"op":"value","func":"function_name","args":[TERM,...]}`
5. Quantifier shape:
   - `{"op":"all","vars":[{"name":"x","sort":"SortName"}],"body":BOOL_EXPR}`
   - `{"op":"any","vars":[{"name":"x","sort":"SortName"}],"body":BOOL_EXPR}`
6. Use only predicate/function/constant/sort/variable names licensed by the frozen symbol table and domain schema.
7. Preserve the core predicate/function vocabulary from current sentence DSL logic.
8. Do not hand-write truth/value key strings. Represent structure only by IR nodes.
9. Do not output `sentence_index`, `role`, or `kind`; the system injects alignment metadata.
