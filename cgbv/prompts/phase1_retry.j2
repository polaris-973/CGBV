You are repairing previously generated Z3-Python code for first-order logic entailment checking.

The natural-language task has not changed. Recompile the same theory object under the constraints below.
Preserve all correct declarations and formulas.

## Retry Context

This is retry attempt {{ attempt_num }} of {{ max_retries }}.

{% if repeated_failure %}
### Escalation Mode

The previous retry repeated the same structural failure. Do NOT make another tiny surface edit that preserves
the same broken shape. Regenerate the affected part cleanly while keeping valid symbols and premise alignment.
{% endif %}

## Original Task

Premises:
{% for premise in premises %}
{{ loop.index }}. {{ premise }}
{% endfor %}

Conclusion: {{ conclusion }}

## Previous Code

```python
{{ previous_code }}
```

## Raw Error

{{ last_error }}

{% if name_error_hint %}
## Specific Diagnosis

{{ name_error_hint }}

{% endif %}
{% if diagnostic %}
## Repair Contract

- Failure stage: `{{ diagnostic.failure_stage or "unknown" }}`
{% if diagnostic.source_slots %}
- Source slots: {% for slot in diagnostic.source_slots %}`{{ slot }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if diagnostic.offending_symbols %}
- Offending symbols: {% for sym in diagnostic.offending_symbols %}`{{ sym }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if diagnostic.preserve_constraints %}
- Must preserve:
{% for line in diagnostic.preserve_constraints %}
  - {{ line }}
{% endfor %}
{% endif %}
{% if diagnostic.forbidden_patterns %}
- Do not repeat:
{% for line in diagnostic.forbidden_patterns %}
  - {{ line }}
{% endfor %}
{% endif %}
{% endif %}
{% if validation_feedback %}
## Validation Feedback

{% for line in validation_feedback %}
- {{ line }}
{% endfor %}

{% endif %}
## Repair Requirements

1. Make the smallest structural change that removes the failure. Preserve all correct declarations and formulas.
2. `premises` must align one-to-one with the numbered NL premises: N premises → exactly N
   top-level formulas in the same order. Do not omit, merge, or split entries (RULE 9).
3. Every identifier used in `premises` or `q` must exactly match a previously declared name (RULE 8).
4. Reuse the existing symbol table whenever possible. Do not introduce new entities, predicates,
   sorts, or bound variables unless the NL explicitly requires them.
5. Do not add a new premise, ground fact, or extra conjunct unless that content is stated in the NL above.
6. If an NL premise is a plain disjunction, keep it as `Or(...)`. Do not rewrite it as
   `And(Or(...), ...)` or otherwise strengthen its meaning.
7. Do not use literal Python `True` or `False` inside `premises` or `q`.
8. Do not include solver calls, print statements, or markdown fences in your answer.

## Self-Check Before Answering

- Python syntax is valid.
- Every name inside `premises` and `q` was declared earlier with identical spelling (RULE 8).
- `premises` contains exactly as many top-level formulas as there are numbered NL premises, in order (RULE 9).
- No new premise, ground fact, or extra conjunct was added unless the NL explicitly states it.
- No disjunction was strengthened into a conjunction.
- `premises` and `q` contain no literal Python `True` or `False`.
- `ForAll`/`Exists` list is non-empty; bound variable is a separate `Const`, not an entity constant (RULES 2, 3).
- Only `DeclareSort(...)` is used; no `IntSort`, `StringSort`, `RealSort`, etc. (RULE 6).
- Synonymous NL phrases reuse the SAME predicate name — no new synonym predicate was introduced (RULE 11).
- "X is a [type]" uses a type predicate `is_type(X)`, never entity equality `X == type_entity` (RULE 12).
- For any clause of the form "not A or B" / "neither A nor B": verify `And(Not(A), Not(B))` was used — not `Or(Not(A), Not(B))`. (RULE 13)

Output only the corrected Python code.
