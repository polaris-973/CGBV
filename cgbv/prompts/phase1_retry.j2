You are repairing a previously generated JSON DSL theory.
The natural-language task has not changed.
Preserve all correct declarations and logical objects.
Output ONLY valid JSON.

## Retry Context

This is retry attempt {{ attempt_num }} of {{ max_retries }}.

{% if repeated_failure %}
The previous retry repeated the same structural failure.
Do not make a cosmetic edit that preserves the same broken shape.
{% endif %}

## Original Task

Premises:
{% for premise in premises %}
{{ loop.index }}. {{ premise }}
{% endfor %}

Conclusion: {{ conclusion }}

## Previous JSON DSL

```json
{{ previous_output }}
```

## Error

{{ last_error }}

{% if diagnostic %}
## Repair Contract

- Failure stage: `{{ diagnostic.failure_stage or "unknown" }}`
{% if diagnostic.source_slots %}
- Affected slots: {% for slot in diagnostic.source_slots %}`{{ slot }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if diagnostic.offending_symbols %}
- Offending symbols: {% for sym in diagnostic.offending_symbols %}`{{ sym }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if diagnostic.preserve_constraints %}
- Preserve constraints:
{% for c in diagnostic.preserve_constraints %}
  - {{ c }}
{% endfor %}
{% endif %}
{% if diagnostic.forbidden_patterns %}
- Forbidden patterns:
{% for p in diagnostic.forbidden_patterns %}
  - {{ p }}
{% endfor %}
{% endif %}
{% endif %}

{% if validation_feedback %}
## Validation Feedback

{% for line in validation_feedback %}
- {{ line }}
{% endfor %}
{% endif %}

## Repair Rules

1. Keep the same compact top-level DSL structure: `symbols`, `sentences`, `query`, `background`.
2. Keep exactly one `logic` string per premise sentence, in the original order.
3. Keep every `nl` text unchanged.
4. Reuse the existing symbol table whenever possible. Do not introduce new names unless the NL requires them.
5. Output the complete corrected DSL theory, not a patch.
6. Each `logic` value must be a compact logic string, not nested AST JSON.
7. Allowed syntax: `p(x)`, `f(x)==y`, `x!=y`, `and(...)`, `or(...)`, `not(...)`, `implies(a,b)`, `iff(a,b)`, `forall x:S. body`, `exists x:S. body`, `eq(a,b)`, `ne(a,b)`.
8. Prefer one object-domain sort `Entity`; encode type/category with unary predicates, not separate object sorts.
9. Use numeric literals only for numeric sorts (`Int`/`Real`/`BitVec`); otherwise use named constants.
10. Identifier names must be safe (`[A-Za-z_][A-Za-z0-9_]*`): no dots/hyphens/spaces.
11. Never use assignment-style `"x = y"`; use `"x == y"`.
12. Never use literal boolean atoms like `"true"` or `"false"`.
13. Do not output legacy wrapper fields like `sentence_index`, `role`, `kind`, `constraint`, `forall`, `exists`.
