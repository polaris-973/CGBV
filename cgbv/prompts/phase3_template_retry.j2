You are repairing one grounded-template IR.

Output ONLY one JSON object.

## Previous Output

```json
{{ previous_output }}
```

## Error

{{ last_error }}

## Frozen Symbol Table

```json
{{ symbol_context_json }}
```

## Current Sentence DSL Logic

```json
{{ sentence_logic_json }}
```

Core predicate/function vocabulary that must remain represented:
{% if core_predicates %}`{{ core_predicates | join("`, `") }}`{% else %}`(none)`{% endif %}

## Domain Schema

{{ domain_schema }}

## Sentence

"{{ sentence }}"

## Sentence Index (Debug Context Only)

- `sentence_index`: `{{ sentence_index }}`

## Reminder

1. Keep top-level structure:
   - `expr` only
2. Fix errors by IR semantics only.
3. Typical errors to fix:
   - undeclared `pred` / `func` / `var`
   - unsupported `op`
   - missing required fields
   - arity mismatch
   - sort mismatch
   - missing required predicate/function from core vocabulary
4. `all/any` must use `vars` + `body` shape.
5. `truth` must use `pred + args`; `value` must use `func + args`.
6. Do not output `sentence_index`, `role`, or `kind`; only repair `expr`.
