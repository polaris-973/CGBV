You are grounding multiple natural-language sentences into grounded-template IR JSON objects.

Output one line per sentence:

```text
[N] {JSON_OBJECT}
```

Do not output Python code.
Do not output Z3 code.

## Frozen Symbol Table

```json
{{ symbol_context_json }}
```

## Domain Schema

{{ domain_schema }}

## Sentences And DSL Logic

{% for sentence in sentences %}
[{{ loop.index }}] "{{ sentence }}"
{% if sentence_logic_pairs and sentence_logic_pairs[loop.index0] %}
Core vocabulary: {% set pair = sentence_logic_pairs[loop.index0] %}{% if pair.core_predicates %}`{{ pair.core_predicates | join("`, `") }}`{% else %}`(none)`{% endif %}
Debug sentence_index: `{{ pair.sentence_index }}`
```json
{{ pair.logic_json }}
```
{% endif %}
{% endfor %}

## Output Rules

1. Output ALL `[N]` lines.
2. Each `[N]` line contains exactly one JSON object with required top-level field:
   - `expr`
3. Use only allowed ops:
   - `and`, `or`, `not`, `implies`, `iff`, `const_bool`
   - `truth`, `value`, `const`, `var`
   - `eq`, `ne`, `lt`, `le`, `gt`, `ge`
   - `all`, `any`
4. Use only names licensed by frozen symbol table and domain schema.
5. Keep each sentence's core predicate/function vocabulary aligned with its DSL logic.
6. `[N]` is only output slot index; do not copy it into JSON fields.
7. Do not output `sentence_index`, `role`, or `kind`; the system injects alignment metadata.
